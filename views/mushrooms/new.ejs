<% layout('layouts/boilerplate') %>
    <h1>So, you found a mushroom?</h1>
    <form action="/mushrooms" method="POST">
        <div>
            <label for="title">Mushroom species</label>
            <input type="text" id="title" name="mushroom[title]" placeholder="Species">
        </div>
        <div>
            <label for="location">Location</label>
            <input type="text" name="mushroom[location]" placeholder="Where did you find it?">
        </div>
        <div>
            <label for="dateFound">Date found</label>
            <input type="datetime-local" id="dateFound" name="mushroom[dateFound]">
        </div>
        <div>
            <p>This mushroom is:</p>
            <label for="edible">edible</label>
            <input type="radio" name="mushroom[edible]" value="true" id="edible">
            <label for="inedible">inedible</label>
            <input type="radio" id="inedible" name="mushroom[edible]" value="false">
        </div>
        <button>Add new Mushroom</button>
    </form>
